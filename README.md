# image-pocessing-integration-tests
A independent, modularized application for integration testing that runs seperately from the image processing api. It uses HTTP requests to test its functionality.
